---
month: 2026-08
---

## 2026-08-05

### [ML] 케라스 학습 파이프라인
#keras #training

- 모델 생성 → compile → fit → evaluate → predict
- compile에서 손실함수, 옵티마이저, 학습률을 설정한다

### [ML] 고전적인 퍼셉트론 vs 로지스틱 회귀 분류기
#퍼셉트론 #TLU #로지스틱회귀

- 퍼셉트론은 활성화함수로 계단함수를 사용한다. 계단함수는 미분 불가능하므로 경사 하강법을 사용해서 손실함수 최소화하는 방법으로 학습할 수 없고, 대신 오분류 시에만 가중치를 조정하는 발견적 규칙으로 학습한다.
- 퍼셉트론을 로지스틱 회귀 분류기와 동등하게 만들기 위해서는 두 가지를 수정해야 한다. 활성화 함수를 계단함수에서 로지스틱함수로 바꾸고, 훈련 방식을 퍼셉트론 학습 규칙에서 cross entropy를 최소화하는 경사 하강법으로 바꾸면 된다.

### [ML] 활성화 함수
#sigmoid #softmax

- 다중 클래스 환경에서 확률을 출력하고 싶을 때는 SoftMax 함수를 사용한다. SoftMax는 모든 클래스들의 확률의 합을 1으로 조절한다.
- SoftMax는 Sigmoid의 다중 클래스 일반화이다.

### [ML] 퍼셉트론의 행렬 수식
#퍼셉트론 #수식

- 선형대수의 일반적 표기인 y=Wx와 달리, 딥러닝에서는 Y=XW를 사용한다. transpose된 행렬을 사용하므로 수학적으로 동일하다. 딥러닝의 경우에서 X: (m × in), W: (in × out)이고 m은 배치의 크기이다.
- Y=XW+b에서, 편향벡터 b는 거의 항상 1차원 벡터다. XW와 크기, 차원이 맞지 않지만 브로드캐스팅으로 행렬의 모든 행에 덧셈이 각기 적용된다. 하나의 뉴런마다 편향이 더해지므로 (1, XW의 열 크기)로 크기가 정해진다.

### [ML] 다층 퍼셉트론의 하이퍼파라미터
#퍼셉트론 #MLP #하이퍼파라미터

- MLP의 하이퍼파라미터는 은닉층 수, 각 은닉층의 뉴런 수, 각 은닉층과 출력층에서 사용하는 활성화 함수가 있다.
- MLP가 훈련 데이터에 과대적합되었다면 은닉층 수와 각 은닉층의 뉴런 수를 줄여볼 수 있다.

---

## 2026-08-06

### [ML] keras.callbacks.Callback 사용
#callback #keras

- 훈련의 중간 지점에 객체 리스트를 호출할 수 있다.
- fit() 메소드의 callback 매개변수로 사용. fit(callbacks=[객체, 객체, ...])
- 호출 가능한 시점: on_train_begin, on_epoch_begin, on_batch_begin, on_batch_end, on_epoch_end, 등등
- 호출 가능한 객체: ModelCheckpoint, EarlyStopping, 사용자 정의 객체 등

### [ML] 직접 정의한 객체로 keras.callbacks.Callback 사용
#callback #keras

- 직접 정의한 객체로 keras의 callback을 사용하려면, keras.callbacks.Callback를 상속한 객체를 선언한다. class MyClass(keras.callbacks.Callback): ...
- keras.callbacks.Callback는 on_epoch_begin, on_batch_end와 같은 여러 메소드를 빈 껍데기로 정의해두고 있으므로 필요한 것을 골라 덮어쓴다. def on_batch_end(self, batch, logs): ...

### [ML] keras.callbacks.History
#keras #history

- fit() 실행하면 History 오브젝트가 출력된다. history = model.fit(...)와 같이 사용
- 주요 속성 history.history: 딕셔너리. {key: loss, accuracy같은 지표 이름, value: 에포크별 값들의 리스트. 길이는 에포크 개수}. 
- 주요 속성 history.epoch: 에포크 인덱스 리스트 [0, 1, 2, ...]
- 주요 속성 history.params: 학습 설정(epochs, steps, verbose 등)
- 주요 속성 history.model: 학습된 모델 객체 참조

---

## 2026-08-07

### [PYTHON] 배열을 벡터화하기
#배열 #벡터화

- 배열에 == 연산자를 걸면, 각 원소가 각 원소와 비교한 값 True/False로 대체된 배열이 반환됨
- y_train이 [5, 0, 4, 1, 5, ...]일때, y_train==5는 [True, False, False, False, True, ...]를 반환함.
- y_train_5 = (y_train ==5)처럼 변수에 받아서 벡터화된 배열을 사용할 수 있다.

### [ML] 성능 측정
#성능측정 #회귀 #분류 #다중분류

- 회귀에서 정답과 예측이 연속적 숫자이므로 얼마나 빗나갔는가를 잰다.
- 회귀의 성능 측정 RMSE: 큰 오차에 민감
- 회귀의 성능 측정 MAE: 이상치에 덜 흔들림
- 회귀의 성능 측정 R제곱: 분산의 몇 %를 설명하는가. 0~1 범위라 스케일에 무관
- 분류의 성능 측정 정확도(accuracy): 전체 중 맞힌 비율
- 분류의 성능 측정 정밀도(precision): 양성이라고 예측한 것 중 실제 양성 비율 TP/(TP+FP). 확실한 것에 대해 확실하게 말하는 정도. 중요한 상황: 아동의 유튜브 피드 알고리즘을 위한 안전 영상 필터 분류기.
- 분류의 성능 측정 재현율(recall): 실제 양성 중 실제 양성이라고 찾아낸 비율 TP/(TP+FN). 진짜인 것들을 얼마나 많이 잡아냈는지. 중요한 상황: 감시 카메라로 도둑을 잡아내는 분류기.
- 분류의 성능 측정 F1 점수: 정밀도와 재현율의 조화 평균
- 정밀도와 재현율은 임계값에 따라 서로 반대로 움직이고, 트레이드오프 곡선을 그리면 ROC 곡선과 AUC가 나온다.
- 다중 분류의 성능 측정: 클래스마다 이진분류 성능측정을 해서 평균을 낸다. 평균 내는 방식은 3개(macro, weighted, micro)다.

### [ML] decision_function() 메소드
#sklearn #threshold #classification

- sklearn 분류기의 predict()는 내부적으로 두가지 일을 한다. 샘플마다 결정 점수를 계산하고, 임계값과 비교해서 클래스를 결정한다. 이때 임계값은 직접 지정할 수 없다.
- 따라서 임계값을 직접 지정하고 싶을때, predict()를 대신 predict() 내부에서 쓰는 결정 함수인 decision_function()를 사용한다.
- 결정 경계에서 얼마나, 어느 쪽으로 떨어져 있는지를 나타내는 점수고, 직접 (score > threshold) 조건으로 판정하고 분류하면 된다.
- 사실 임계값을 직접 지정하고 싶으면 TunedThresholdClassifierCV를 쓰면 된다. 하지만 decision_function()에 대해 알아두자.

### [PYTHON] 최대공약수 구하기
#gcd #최대공약수

- 큰 수를 작은 수로 나눈 mod인 a%b를 구하고, 나머지가 0이 될 때까지 a, b = b, a%b를 반복한다.

---

## 2026-08-08

### [STAT] 혼동 행렬
#confusionmatrix #sensitivity #specificity #ROC

- 민감도(sensitivity) = 재현율 = True Positive Rate(TPR) = TP / (TP+FN)
- 민감도가 중요한 상황: 도둑 잡을때. 잘못 출동하더라도 도둑을 놓치는 일은 없도록
- 특이도(specificity) = True Negative Rate(TNR) = TN / (FP+TN) = 실제 아닌것을 아니라고 말하는 게 중요한 경우
- 특이도가 중요한 상황: 교통 카메라에서 과속하는 차를 놓치더라도 과속 안하는 차를 과금하는 일이 없도록

### [STAT] ROC 곡선
#ROC #PR

- ROC는 정밀도에 대한 재현율 곡선이 아니고, 민감도(=재현율)에 대한 1-특이도 곡선이다.
- 1-특이도 = 1-TNR = FPR이므로, TPR에 대한 FPR 곡선이라 할 수 있다.
- TPR = f(TP, FN)이고 FPR = g(FP, TN)이므로, 혼동 행렬의 모든 네개의 값이 사용된다.
- TPR은 실제양성에 대해, FPR는 실제음성에 대해 산출된 값이다.
- PR 곡선은 TN을 아예 사용하지 않으므로, TN 값이 압도적으로 많을 때(5-분류기 등) PR을 사용한다. 이 경우 ROC를 사용하면 성능을 낙관적으로 보이게 한다.

### [STAT] PR vs ROC #review
#PR #ROC

- 양성 클래스가 드물거나 FP가 FN보다 중요하면 PR 곡선을, 그렇지 않으면 ROC 곡선을 선택한다.
- 아직 헷갈리는 부분: 양성 클래스가 드물 때 PR을 쓰라는 건 알겠는데, PR이 ROC 대비 FN보다 FP를 더 강조한다는 것은 이해가 안 된다.

### [ML] decision_function()과 predict_proba()
#decisionfunction #predictproba #method

- decision_function(): 결정 경계와의 거리를 반환한다. 출력값이 ([2164.22, -698.37])처럼 큰 수가 나올 수 있으므로 값의 스케일에 절대적 의미가 없고, 다른 샘플들의 점수 분포를 봐야 한다. 출력 형태는 1차원 배열이다.
- predict_proba(): 다중 클래스 분류기에서, 샘플이 각 클래스에 해당할 0~1 사이 확률을 출력한다. 출력 형태는 2차원 배열로, 각 행이 각 샘플이고 열은 클래스들이다. 열의 순서는 classes_ 속성이 정한다.

| 분류기 | decision_function | predict_proba |
| --- | --- | --- |
| LogisticRegression | 있음 | 있음 |
| SGDClassifier | 있음 | loss에 따라 |
| SVC | 있음 | probability=True일 때만 |
| RandomForest, GradientBoosting | 없음 | 있음 |
| DecisionTree, KNeighbors | 없음 | 있음 |
| GaussianNB | 없음 | 있음 |

- **SGDClassifier**가 조건부인 이유는 손실 함수에 달려 있다. loss="log_loss"면 확률적 모델이 되어 predict_proba가 생기고, 기본값인 loss="hinge"면 확률 개념이 없어 decision_function만 있다.
- **SVC**도 마찬가지다. SVM은 확률을 내지 않는 알고리즘이라, 확률이 필요하면 probability=True로 Platt scaling을 해야 한다.

### [PYTHON] list comprehension
#배열

- 배열변수 = [표현식 for 변수 in 반복대상 if 조건]
```python
nums = [1, 2, 3, 4]
squares = [x*x for x in nums]
# [1, 4, 9, 16]
```
- 이때 대입(초기화)되는 게 아니라 append 되는 것이다!!

---

## 2026-08-09

### [PYTHON] range(범위)
#range

- range(범위)에서 범위가 빈 값이면 range에 포함되지 않는다.
- max(s1, s2) < min(e1, e2)인 조건 위해 작성한 range([max(s1, s2), min(e1, e2)])에서, max >= min이면 range가 빈 값이 되어 오류없이 자동으로 걸러진다. 

### [PYTHON] set
#set

- 선언 s = set()
- 합집합 a | b
- 교집합 a & b
- 차집합 a - b

### [PYTHON] itertools.combinations
#combinations

- from itertools import combinations
- combinations(iterable, r). 예를 들면 combinations(['a', 'b', 'c'], 2)
- 꺼내 쓰려면 반드시 list(combinations(...))으로 접근해야 하며, 한번 순회하면 소진되어 다시 list(combinations(...))하면 아무것도 나오지 않는다.
- 결과: [('a', 'b'), ('a', 'c'), ('b', 'c')]. 순서가 다른 것은 같은 것으로 취급. 순서까지 구분하려면 permutations 사용할것.


### [DSA] 겹치는 선분의 길이 (Lv.0, 정답률 62%)
#set

- 겹치는 구간을 [max(s1, s2), min(e1, e2)]로 정의하고, set으로 중복제거된 총 범위를 구한다.
- 조합 가능한 모든 쌍을 고려해야 한다.
- 시간 O(n^2 * C) / 공간 O(C)

```python
from itertools import combinations

def solution(lines):
    covered = set()
    for (s1, e1), (s2, e2) in combinations(lines, 2):
        # 겹치는 구간: range(max(s1, s2), min(e1, e2))
        covered |= set(range(max(s1, s2), min(e1, e2)))
    return len(covered)
```

### [PYTHON] Counter
#counter

- from collections import Counter
- iterable의 원소들의 개수를 세서 hashset으로 저장한다. key: 원소, value: 개수
- counter = Counter(배열)   # {'apple': 3, 'banana': 2, 'cherry': 1}
- counter.most_common(2)    # top 2의 hashset 반환. {'apple': 3, 'banana': 2}
- 없는 원소 접근하면 0 출력. counter["orange"] # 0
- counter.total()   # 6
- Counter끼리 더하기: c1 + c2를 하면 일치하는 원소끼리 값을 더한다.
- Counter 값에 추가: counter.update(["a", "b", "b"])

### [PYTHON] list.sort() vs sorted()
#sort #sorted

- sort(): 배열 원본을 변경하는 list의 함수. my_list.sort()
- sorted(): built-in 함수. iterable을 입력받고 새로운 리스트을 반환한다. new_list = sorted(iterable)

### [PYTHON] array vs list
#배열 #리스트

- 배열: 크기가 고정됨. 한 종류의 원소를 가짐.
- 리스트: 크기가 동적임. 다양한 종류의 원소를 가질 수 있음. 인덱스로 접근 가능. 

### [DSA] 등수 매기기 (Lv.0, 정답률 77%)
#array #indexmapping

- 인덱스에 너무 신경쓸 필요 없이, 각 총점수가 몇등에 해당하는지만 hashmap으로 저장하면 된다.
- 시간 O(nlogn) / 공간 O(n)

```python
totals = [sum(s) for s in scores]

rank = {}

# 총점수가 몇등에 해당하는지 저장
# totals에 대해 순회
for i, t in enumerate(sorted(totals)):
    # 본 적 없는 점수에 대해서만 저장
    if t not in rank:
        rank[t] = i+1

return [rank[t] for t in totals]
```

---

## 2026-08-12

### [PYTHON] numpy axis 연산
#numpy #axis #keepdims

```python
# 이 코드 이해하기
row_sums = conf_mx.sum(axis=1, keepdims=True)
norm_conf_mx = conf_mx / row_sums
```

```text
axis=1 방향 (좌→우) ──────────>
axis=0  [ 80,  10,  10 ]    # 더하면 100
방향    [  5,  85,  10 ]       # 더하면 100
(위→아래) [  2,   3,  15 ]      # 더하면 20
   │
   V
```

- axis=1을 따라 이동하면 한 행 안에서 좌우로 움직인다. 그러면서 더하니까 **한 행의 합**이 나온다.
- keepdims=True를 설정해야 shape가 (3,)에서 (3, 1)으로 제대로 변환된다.
- keepdims 없이는, 뒤쪽 축부터 맞추므로 shape (1, 3)의 배열이 된다.

```text
[[80, 10, 10],      [[100, 100, 100],
 [ 5, 85, 10],   ÷   [100, 100, 100],     ← 행 방향으로 나눠짐
 [ 2,  3, 15]]       [ 20,  20,  20]]
```

### [PYTHON] pyplot.subplot
#matplotlib #pyplot #subplot

- 참고: 옛날 방식이다.

```python
plt.subplot(221)
```

- plt.subplot(2, 2, 1)와 완전히 동일한, 축약된 버전이다.
- subplot(전체 행수, 전체 열수, 그중 몇번째 칸에 그릴지)

- 요즘은 이렇게 더 많이 쓴다.
```python
fig, axes = plt.subplots(2, 2, figsize=(8, 8))  # subplots 끝에 s 붙음 주의
axes[0, 0].imshow(...)
axes[0, 1].imshow(...)
```


### [PYTHON] sklearn.metrics.f1_score(average=binary)
#sklearn #metrics #f1_score #average

- 클래스나 레이블이 여러 개일 때, 각각 계산된 점수를 어떻게 하나로 합칠지 정한다.
- 다중 분류기를 f1_score로 평가할때, 필수로 average 값을 입력해야 한다.
- average의 기본값은 이진 분류를 위한 'binary'이다.
- 'macro': 레이블별 점수를 단순 평균. 샘플이 적은 레이블도 큰 레이블과 같은 비중을 갖는다.
- 'weighted': 레이블별 점수를 지지도로 가중 평균. 지지도(support): 해당 레이블에 속하는 샘플의 개수.
- 'micro': 전체 TP, FP, FN을 먼저 합산한 뒤 점수 계산. 레이블별 점수 계산이 아니라, 모든 레이블에 대한 연산이다.
- 'None': 합치지 않고 레이블별 점수 배열 반환

### [ML] KNN
#knn

- KNN은 훈련이 싸고 예측이 비싸다. fit()할때 데이터를 저장할 뿐이고, 대신 예측 한 번마다 훈련 세트 전체와 거리를 계산한다.
- KNN이 MNIST와 잘 맞는 이유: 픽셀 거리가 의미 있는 유사도이다. 모든 피처의 단위와 범위가 0~255 픽셀 강도로 같다. 

---

## 2026-08-16

### [PYTHON] sort(key=), sorted(key=)
#정렬 #sort #sorted #key #lambda함수

- key 파라미터로 정렬조건을 설정할 수 있다.
```python
# 내장함수 len: 길이 짧은 순으로 정렬
a = ("Jennifer", "Sally", "Jane")
print(sorted(a, key=len))   # ['Jane', 'Sally', 'Jenifer']

# n에 가까운 순서로 정렬하기
numlist.sort(key=lambda x: abs(x - n))
```

### [DSA] 특이한 정렬: 정렬 기준이 두 개 이상일때  (Lv.0, 정답률 75%)
#정렬 #튜플

```python
# 풀이 1: 튜플 비교
def solution(n, numlist):
    return sorted(numlist, key=lambda x: (abs(x - n), -x))
```

- 튜플이 들어있는 리스트 반환. (n과의 거리, 값의 마이너스)
- sorted()가 튜플을 작은 순부터 정렬하므로 거리 가까운 순, 거리 같을때 값이 큰순으로 정렬된다.
- 시간 O(nlogn) / 공간 O(n)

```python
# 풀이 2: 정렬 두번하기
def solution(n, numlist):
    numlist.sort(reverse=True)  # 값 기준 내림차순
    numlist.sort(key=lambda x: abs(x - n))  # 거리 기준 오름차순
    return numlist
```
- 거리 기준 정렬할 때, 값 기준 정렬은 순서가 유지됨
- 시간 O(nlogn) / 공간 O(1)

### [PYTHON] split
#문자열 #split

```python
text = "Python is fun"
print(text.split()) # split by whitespace가 디폴트
# ['Python', 'is', 'fun']
```
```python
data = "apple,banana,cherry"
print(data.split(","))
# ['apple', 'banana', 'cherry']
```

### [PYTHON] sqrt vs isqrt
#math #sqrt #isqrt

- math.isqrt(n)는 ⌊√n⌋, 즉 n의 제곱근을 내림한 정수를 반환한다.
- math.sqrt(n)은 float를 반환하므로, 큰 수를 다룰 때 부동소수점 오차를 발생시킬 수 있다.

### [PYTHON] str의 isdecimal()
#문자열

- 문자열 자체함수 isdecimal()이 있으므로 한글자씩 순회할 필요 없다.
```python
# 모든 글자가 0-9 사이 숫자인지
return string.isdecimal()
```

---

## 2026-08-17

### [PYTHON] join()을 생활화합시다!
#문자열 #join

- ''.join(...)의 사용을 생활화합시다.
- 문자열을 이어붙여 만들어야 할때 join부터 떠올리기!

### [PYTHON] itertools.combinations vs math.comb()
#itertools #combinations #math #comb

- itertools.combinations는 [튜플, 튜플, 튜플]처럼 실제 조합들을 반환하고, math.comb()는 조합의 개수만 반환한다.
- 실제 조합들이 필요한 게 아니라면, len(list(combinations(iterable, r))) 대신 math.comb(n, r)를 쓰자.

### [PYTHON] enumerate(string)을 생활화합시다!
#문자열 #enumerate

- 문자열을 순회하면서 인덱스도 쓰고 각 글자도 쓸 때 enumerate를 떠올리자!

### [DSA] 숨어있는 숫자의 덧셈 (2) (Lv.0, 정답률 86%)
#문자열

- 숫자의 자릿수를 트래킹하며 문자열의 substring을 정수로 변환하기
- 시간 O(n) / 공간 O(1)

```python
# 내 풀이. 가독성이 떨어진다는 지적
def solution(my_string):
    answer = 0
    temp = 0

    for i in range(len(my_string)):
        if my_string[i].isdecimal():
            # 다음 글자도 숫자일때, 이번글자 * 10 해서 'nnn0' 꼴을 temp에 저장한다.
            if i < len(my_string)-1 and my_string[i+1].isdecimal():
                temp *= 10
                temp += int(my_string[i]) * 10
            # 이번 글자가 한 숫자의 마지막 글자였을때, 정답값에 temp와 이번글자(digit)을 더한다.
            else:
                answer += temp + int(my_string[i])
                temp = 0
```


```python
# 클로드가 보완한 내 풀이
def solution(my_string):
    answer = 0
    temp = 0
    # enumerate을 써서 my_string[i]를 ch 대체
    for i, ch in enumerate(my_string):
        if ch.isdecimal():
            # 이번 글자가 숫자면, 다음 글자를 보기 전에 지금까지의 숫자를 만든다.
            temp = temp * 10 + int(ch)
            # 이번 글자가 마지막 글자였을 경우, 정답값에 temp를 더한다.
            if i + 1 == len(my_string) or not my_string[i+1].isdecimal():
                answer += temp
                temp = 0
    return answer
```

### [PYTHON] 제너레이터 표현식(list comprehension)
#제너레이터 #문법

```python
# if만 있을때, if가 뒤에 옴
return [x for x in range(10) if x%2==0]

# if-else일때, if와 else가 앞에 오고 for가 뒤에 옴
return [x if x%2==0 else -x for x in range(10)]
```

### [PYTHON] list.index(value)
#list #index

- list.index(value)는 값이 처음 나타나는 인덱스를 반환한다.
```python
# 문제명: 서울에서 김서방 찾기
def solution(seoul):
    return f"김서방은 {seoul.index("Kim")}에 있다"
```

---

## 2026-08-23

### [DSA] heap
#heap #heapq

- 가장 작은 값을 꺼내야 하는 상황에서 쓴다.
- 힙은 정렬된 게 아니라, '부모가 자식보다 작다'만 지켜지는 부분 순서 구조다. h[0]이 항상 최솟값이라는 것만 보장된다.
- 삽입: O(logn), 꺼내기: O(logn), heapify: O(n)
```python
# 기본 API
import heapq

h = []
heapq.heappush(h, 5)    # 삽입
heapq.heappush(h, 1)
heapq.heappush(h, 3)

print(h[0])     # 결과: 1. 꺼내지 않고 최솟값만 확인
print(heapq.heappop(h)) # 결과: 1. 최솟값 제거하고 반환

nums = [5, 1, 3, 9, 2]
heapq.heapify(nums) # 기존 리스트를 제자리에서 힙으로 변환
```

```python
# 가끔 쓰는 것들
heapq.heappushpop(h, x) # push 후 pop (x가 최솟값이면 그대로 반환)
heapq.heapreplace(h, x) # pop 후 push (힙이 비어있으면 에러)
heapq.nlargest(3, nums) # 상위 3개 (힙 안 만들고 바로. heapify 필요 없음)
heapq.nsmallest(3, nums)
```

```python
# 최대 힙이 필요하면 부호를 뒤집어서 쓴다.
h = []
for x in nums:
    heapq.heappush(h, -x)   # -x를 push
biggest = -heapq.heappop(h) # 꺼낼 때 다시 뒤집기
```

### [DSA] zip
#zip

- zip은 iterator이다.
- 여러 시퀀스를 **세로로** 묶어서 **튜플** 하나씩 반환한다.
- zip은 리스트가 아니라 iterator이므로, 두 번 이상 순회해야 하면 list(zip(...))으로 변수에 담아둘 것.

```python
# 기본 사용
nums = [1, 2, 3, 4]
chars = ['a', 'b', 'c']

for n, c in zip(numbs, chars):
    print(n, c)     # 1 a / 2 b / 3 c
# 4에 대응하는 char의 원소가 없으므로, zip에 묶이지 않고 버려진다.
# 버려지지 않게 하려면, itertools.zip_longest 사용
```

```python
# 자주 쓰는 패턴: zip(*matrix)으로 행렬의 transpose 만들기
matrix = [[1, 2, 3],
          [4, 5, 6]]

cols = list(zip(*matrix))
# [(1, 4), (2, 5), (3, 6)]  ← 열이 행이 됨
```

```python
# 자주 쓰는 조합

## 딕셔너리 만들기
keys = ['a', 'b', 'c']
vals = [1, 2, 3]
d = dict(zip(keys, vals))   # {'a': 1, 'b': 2, 'c': 3}

## 두 리스트를 함께 정렬 (한쪽 기준)
names = ['철수', '영희', '민수']
scores = [80, 95, 70]
ranked = sorted(zip(scores, names), reverse=True)
# [(95, '영희'), (80, '철수'), (70, '민수')]

## 인접한 원소 쌍 (슬라이딩 윈도우)
for prev, cur in zip(nums, nums[1:]):
    print(cur - prev)
```

### [PYTHON] set
#set

- set(...)을 써서 해도 되고, {...}으로 set 컴프리헨션 문법을 쓸 수 있다.

---

## 2026-08-26

### [PYTHON] dict
#dict #hashmap

```python
# 배열 2개로 dict 만들기
scores = dict(zip(name, yearning))  # '추억 점수' 문제

# 배열로 dict 만들기, index를 key로
my_dict = {i: v for i, v in enumerate(list)}
```

- 찾을 때 my_dict.get(person, 0)으로
- 업데이트할때 my_dict.update(another_dict) 또는 my_dict.update(age=26, city="Seoul)

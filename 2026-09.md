---
month: 2026-09
---

## 2026-09-02

### [DSA] 약수 세는 방법, 체(Sieve) 방식
#약수

- 1부터 N까지 모든 수에 대해 약수 관련 값이 필요할 때 사용
- 수 하나의 약수가 필요한 경우에는 체 대신 O(√n)의 방법 사용
- 복잡도 비교. O(√n)를 n번 수행하면 시간 O(n√n) 공간 O(1), Sieve는 시간 O(nlogn) 공간 O(n)

```text
i=1 →  1  2  3  4  5  6  ...   (전부)
i=2 →     2     4     6  ...
i=3 →        3        6  ...
```

```python
# 1 ~ N의 숫자들의 약수 개수 
def count_divisors_upto(N):
    divisor = [0] * (N + 1)
    for i in range(1, N + 1):
        for j in range(i, N+1, i):
            divisors[j] += 1    # 필요에 따라 이 부분 수정
    return divisors
```

### [PYTHON] 딕셔너리에 없으면 새 값, 딕셔너리에 있으면 비교해서 더 작은값
#tip

- 프로그래머스 '대충 만든 자판' 문제 참조.

```python
min(i, cost.get(c, math.inf))    # 딕셔너리에 없으면 무한대 반환, 무조건 j가 더 작게끔
```

```python
min(i, cost.get(c, i))  # math.inf 대신 i를 써도 같은 결과가 나온다.
```

### [PYTHON] skip 플래그 대신 중첩 함수 사용
#tip

- 프로그래머스 '대충 만든 자판' 문제 참조.

```python
# 기존 코드
skip = False
for c in target:
    if c not in keydict.keys():
        skip = True     # 더이상 순회하지 않아도 되며, target에 대한 값은 -1으로 확정됨
        break
    count += keydict[c]
if skip:
    answer.append(-1)
```

```python
def solution(keymap, targets):
    ...
    # 저장
    for key in keymap:
        ...
    
    # 출력할 리스트의 값 생성
    def press(target):
        for c in target:
            if c not in cost:
                return -1   # 바로 return해버려서 함수에서 빠져나옴. skip 플래그 불필요
            total += cost[c]
        return total
    return [press(t) for t in targets]
```

### [PYTHON] for...else 문
#forelse

- 비추천하는 문법이지만, 참고로 쓴다. 적합한 경우가 한정적이고 모르는 사람이 많다.
- 루프를 다 돌고도 루프 안의 break가 실행되지 않았을 때 else 조건이 성립한다.
```python
for x in items:
    if 조건(x):
        break
else:   # else가 아닌 'nobreak'라고 생각하면 쉽다.
    처리()
```

